const KEY='experiment-journal-v3',DB='experimentJournalMediaV3',STORE='files';
const base={activeId:'exp1',experiments:[
{id:'exp1',num:'01',kicker:'CONSISTENCY',title:'감정을 캐릭터로 만들며, 일관성을 설계하다',date:'2026-08-11',summary:'직장인 디자이너의 감정을 의인화한 Fluffy Fur 3D 캐릭터 12종. 여러 결과물이 하나의 세트처럼 보이도록 만드는 규칙을 실험했습니다.',question:'서로 다른 12개의 감정을 표현하면서도 하나의 캐릭터 시스템처럼 보이게 할 수 있을까?',tried:'형태·재질·조명·앵글·배경·톤을 고정하고 감정별 포즈와 표정만 가변 요소로 두었습니다.',friction:'개별 생성할수록 털 재질과 체형이 달라지고 색이 흔들렸습니다.',applied:'한 컷에 3×4 세트를 한 번에 생성하고 HEX 색상값을 지정했습니다.',learned:'생성형 AI의 일관성은 무엇을 고정하고 무엇을 바꿀지 설계하는 문제였습니다.',next:'동일 캐릭터를 모션으로 확장해도 정체성이 유지되는지 실험하기.',media:[],coverId:null},
{id:'exp2',num:'02',kicker:'IDENTITY',title:'나만의 캐릭터 IHIRI, 일상의 언어가 되다',date:'2026-08-29',summary:'회사 이사, 카페, 팀워크처럼 제 일상의 장면을 하나의 캐릭터로 이어보았습니다.',question:'얼굴·헤어·비율을 유지하면서 상황과 행동만 자연스럽게 바꿀 수 있을까?',tried:'CHARACTER / HAIR / PROPORTION LOCK을 두고 상황 요소만 제한적으로 변화시켰습니다.',friction:'생성할 때마다 미니미 비율이 흔들리고 영상에서는 원본 인상이 달라졌습니다.',applied:'고정 요소와 변동 요소를 분리해 장면과 모션으로 확장했습니다.',learned:'정체성과 상황 요소를 분리하는 것이 캐릭터 일관성의 핵심이었습니다.',next:'장면 간 캐릭터 지문을 안정적으로 동기화하기.',media:[],coverId:null},
{id:'exp3',num:'03',kicker:'SYSTEM',title:'Zotero MCP로 논문·PDF 리서치 흐름을 만들다',date:'2026-09-10',summary:'AI가 자료를 읽고 정리하는 구조를 만들었습니다. Zotero, Codex MCP, Notion의 역할을 분리해 연결했습니다.',question:'전문 자료를 출처를 보존하면서 재사용 가능한 흐름으로 만들 수 있을까?',tried:'Zotero → Codex MCP → 구조화 → Notion → 실무 활용의 흐름을 설계했습니다.',friction:'MCP 서버 실행과 앱에서 실제 도구를 연결하는 일을 처음에는 같은 것으로 생각했습니다.',applied:'원문은 Zotero, 분석과 활용 기록은 Notion으로 역할을 구분했습니다.',learned:'AI 자동화는 도구 실행보다 실제 업무 환경에 연결하고 운영 규칙을 만드는 것이 중요했습니다.',next:'정기 수집과 팀별 인사이트 제작 루프로 확장하기.',media:[],coverId:null}]};
const fields=[['title','실험 제목','input'],['kicker','키워드','input'],['date','날짜','input'],['summary','한 줄 요약','textarea'],['question','문제 / 목표','textarea'],['tried','무엇을 시도했나요?','textarea'],['friction','어디서 막혔나요?','textarea'],['applied','무엇을 바꿨나요?','textarea'],['learned','무엇을 배웠나요?','textarea'],['next','다음 실험','textarea']];
let data=(()=>{try{return JSON.parse(localStorage.getItem(KEY))||structuredClone(base)}catch{return structuredClone(base)}})(),active=data.activeId||'exp1';
const exp=()=>data.experiments.find(x=>x.id===active),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),nl=s=>esc(s).replace(/\n/g,'<br>');
function persist(silent=false){data.activeId=active;localStorage.setItem(KEY,JSON.stringify(data));if(!silent)toast('저장되었습니다')}
function go(id){document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'})}window.go=go;
function render(){nav();timeline();sections();editor()}
function nav(){document.getElementById('nav').innerHTML=data.experiments.map(e=>`<button class="${e.id===active?'active':''}" onclick="selectExp('${e.id}')"><small>${e.num}</small><span><b>${esc(e.kicker)}</b><em>${esc(e.title)}</em></span></button>`).join('')}
function timeline(){document.getElementById('timeline').innerHTML=data.experiments.map(e=>`<div class="step"><small>${e.num}</small><b>${esc(e.kicker)}</b><span>${esc(e.title)}</span></div>`).join('')}
const src=m=>m?.objectUrl||m?.src||'',cover=e=>e.media.find(m=>m.id===e.coverId)||e.media[0]||null;
function mediaHTML(m){const s=src(m);return s?`<div class="media">${m.type==='video'?`<video src="${s}" muted loop playsinline controls></video>`:`<img src="${s}" alt="">`}<div class="cap">${esc(m.label||'Media')}${m.id===exp().coverId?' · 대표':''}</div></div>`:''}
function sections(){document.getElementById('sections').innerHTML=data.experiments.map(e=>{const c=cover(e),s=src(c),flow=e.id==='exp3'?`<div class="flow"><div class="node">웹·논문·PDF</div><div class="arr">→</div><div class="node">Zotero</div><div class="arr">→</div><div class="node">Codex + MCP</div><div class="arr">→</div><div class="node">Notion</div><div class="arr">→</div><div class="node">업무 활용</div></div>`:'';return `<section class="section" id="${e.id}"><div class="head"><div><div class="eyebrow">실험 ${Number(e.num)}. ${esc(e.kicker)}</div><h2>${esc(e.title)}</h2></div><div class="date">${esc(e.date)}</div></div><article class="card"><div class="cover">${s?(c.type==='video'?`<video src="${s}" muted loop playsinline controls></video>`:`<img src="${s}" alt="">`):`<div class="empty">이 실험의 대표 이미지를 선택해 주세요.<br>각 실험의 이미지는 서로 독립적으로 관리됩니다.</div>`}<div class="badge">${esc(e.kicker)}</div></div><div class="body"><p class="lead">${nl(e.summary)}</p><div class="stories"><div class="story"><label>Question</label><p>${nl(e.question)}</p></div><div class="story"><label>Try</label><p>${nl(e.tried)}</p></div><div class="story"><label>Friction</label><p>${nl(e.friction)}</p></div><div class="story"><label>Change</label><p>${nl(e.applied)}</p></div><div class="story"><label>Learned</label><p><strong>${nl(e.learned)}</strong></p></div><div class="story"><label>Next</label><p>${nl(e.next)}</p></div></div>${flow}${e.media.length?`<div class="gallery">${e.media.map(mediaHTML).join('')}</div>`:''}</div></article></section>`}).join('')}
function editor(target='editor',mgr='manager'){const e=exp(),el=document.getElementById(target);if(!el)return;el.innerHTML=fields.map(([k,l,t])=>`<div class="field"><label>${l}</label>${t==='textarea'?`<textarea data-k="${k}">${esc(e[k])}</textarea>`:`<input data-k="${k}" value="${esc(e[k])}">`}</div>`).join('');el.querySelectorAll('[data-k]').forEach(i=>i.oninput=()=>{e[i.dataset.k]=i.value;nav();timeline();sections()});manager(mgr)}
function manager(target='manager'){const e=exp(),el=document.getElementById(target);if(!el)return;el.innerHTML=`<div class="manager-head"><b>미디어 관리</b><span>대표 / 순서 / 삭제</span></div>${e.media.length?`<div class="media-list">${e.media.map((m,i)=>`<div class="row"><div class="thumb">${m.type==='video'?`<video src="${src(m)}" muted></video>`:`<img src="${src(m)}" alt="">`}</div><div><div class="mname">${esc(m.label||`미디어 ${i+1}`)}</div><div class="msub">${m.id===e.coverId?'<span class="tag">대표 이미지</span>':'업로드 미디어'}</div><div class="macts"><button class="btn tiny" onclick="setCover('${m.id}')">대표로</button><button class="btn tiny" onclick="moveMedia('${m.id}',-1)">위</button><button class="btn tiny" onclick="moveMedia('${m.id}',1)">아래</button><button class="btn tiny" onclick="removeMedia('${m.id}')">삭제</button></div></div></div>`).join('')}</div>`:`<div class="emptybox">이미지나 영상을 추가해 주세요.</div>`}`}
function selectExp(id){active=id;persist(true);render();setTimeout(()=>go(id),30)}window.selectExp=selectExp;
function setCover(id){exp().coverId=id;persist(true);sections();manager();toast('대표 이미지를 바꿨어요')}window.setCover=setCover;
function moveMedia(id,d){const e=exp(),i=e.media.findIndex(m=>m.id===id),n=i+d;if(i<0||n<0||n>=e.media.length)return;const [m]=e.media.splice(i,1);e.media.splice(n,0,m);persist(true);sections();manager()}window.moveMedia=moveMedia;
function removeMedia(id){const e=exp();e.media=e.media.filter(m=>m.id!==id);if(e.coverId===id)e.coverId=e.media[0]?.id||null;persist(true);sections();manager();toast('미디어를 삭제했어요')}window.removeMedia=removeMedia;
function toast(t){const el=document.getElementById('toast');el.textContent=t;el.classList.add('show');clearTimeout(el._t);el._t=setTimeout(()=>el.classList.remove('show'),1600)}
function modal(h){document.getElementById('modalCard').innerHTML=h;document.getElementById('modal').classList.add('show')}document.getElementById('modal').onclick=e=>{if(e.target.id==='modal')e.currentTarget.classList.remove('show')};
function pickExp(){modal(`<h3>Experiments</h3><p>열 실험을 선택하세요.</p>${data.experiments.map(e=>`<button class="btn" style="margin:4px" onclick="document.getElementById('modal').classList.remove('show');selectExp('${e.id}')">${e.num} · ${esc(e.kicker)}</button>`).join('')}`)}window.pickExp=pickExp;
function mobileEdit(){modal(`<h3>Quick Write</h3><p>실험 내용을 수정하고 이미지·영상을 추가할 수 있습니다.</p><div id="mEditor"></div><div class="upload"><button class="btn" onclick="document.getElementById('mobileMedia').click()">이미지·영상 선택</button></div><div id="mManager"></div><button class="btn primary" style="width:100%;margin-top:16px" onclick="persist();document.getElementById('modal').classList.remove('show')">저장</button>`);editor('mEditor','mManager')}window.mobileEdit=mobileEdit;
function tools(){modal(`<h3>Tools</h3><p>기기 간 데이터 이동은 JSON 백업을 이용합니다.</p><button class="btn" onclick="backup()">JSON 백업</button> <button class="btn" onclick="document.getElementById('jsonInput').click()">JSON 불러오기</button>`)}window.tools=tools;
function backup(){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='experiment-journal-backup.json';a.click()}document.getElementById('backupBtn').onclick=backup;document.getElementById('restoreBtn').onclick=()=>document.getElementById('jsonInput').click();
function db(){return new Promise((ok,no)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE);r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)})}async function put(file){const d=await db(),k='m_'+Date.now()+'_'+Math.random().toString(36).slice(2);await new Promise((ok,no)=>{const t=d.transaction(STORE,'readwrite');t.objectStore(STORE).put(file,k);t.oncomplete=ok;t.onerror=()=>no(t.error)});return k}async function url(k){const d=await db(),b=await new Promise((ok,no)=>{const r=d.transaction(STORE).objectStore(STORE).get(k);r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)});return b?URL.createObjectURL(b):''}
async function addFiles(list){const e=exp();for(const f of list){const key=await put(f),m={id:'m_'+Date.now()+Math.random().toString(36).slice(2,6),key,type:f.type.startsWith('video/')?'video':'image',label:f.name};m.objectUrl=await url(key);e.media.push(m);if(!e.coverId)e.coverId=m.id}persist();sections();manager();if(document.getElementById('mManager'))manager('mManager')}
document.getElementById('mediaInput').onchange=async e=>{await addFiles(e.target.files);e.target.value=''};document.getElementById('mobileMedia').onchange=async e=>{await addFiles(e.target.files);e.target.value=''};
document.getElementById('jsonInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{data=JSON.parse(await f.text());active=data.activeId||data.experiments[0].id;persist();await hydrate();render()}catch{alert('백업 파일을 읽지 못했습니다.')}};
async function hydrate(){for(const e of data.experiments)for(const m of e.media||[])if(m.key)try{m.objectUrl=await url(m.key)}catch{}}
document.getElementById('save').onclick=()=>persist();document.getElementById('addExp').onclick=()=>{const n=data.experiments.length+1,id='exp'+Date.now();data.experiments.push({id,num:String(n).padStart(2,'0'),kicker:'NEW EXPERIMENT',title:'새로운 실험',date:new Date().toISOString().slice(0,10),summary:'',question:'',tried:'',friction:'',applied:'',learned:'',next:'',media:[],coverId:null});active=id;persist(true);render();go(id)};
(async()=>{await hydrate();render()})();
